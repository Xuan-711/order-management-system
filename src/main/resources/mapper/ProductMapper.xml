<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.order.mapper.ProductMapper">

    <select id="selectById" resultType="com.xuan.order.entity.Product">
        SELECT id, name, stock, price, create_time
        FROM product
        WHERE id = #{id}
    </select>

    <!-- 乐观锁扣库存: stock >= qty 才更新 -->
    <update id="deductStock">
        UPDATE product
        SET stock = stock - #{qty}
        WHERE id = #{id} AND stock >= #{qty}
    </update>

</mapper>
